Parameter | Type | Description
--------- | ------- | -----------
address | string |  wallet address string
amount | - | <% if with_optional_params %>(Optional)<% end %>  the integer amount to receive, in **atomic** units
payment_id | string | <%if with_optional_params %>(Optional)<% end %>  16 or 64 character hexadecimal payment id
recipient_name | string| <%if with_optional_params %>(Optional)<% end %>  name of the payment recipient
tx_description | string | <%if with_optional_params %>(Optional)<% end %>  tx reason description
